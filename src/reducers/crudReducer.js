const initialState = {
   friends: [],
   isFetching: false,
   isEditing: false,
   friendToEdit: {},
   error: '',
}

export const crudReducer = (state = initialState, action) => {
   switch (action.type) {
      default:
         return state
   }
}